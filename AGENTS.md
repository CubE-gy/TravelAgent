# TravelAgent V1 协作约定

## 1. Codex 与用户的协作方式

- 使用中文沟通，代码、命令、类型名等技术标识符保留英文。
- 一个 Stage 是一个可独立验收的业务能力闭环，原则上使用一个独立 Chat 完成。
- 严格按 Stage 0 → 9 推进，不跨 Stage 提前开发未来业务。
- 用户负责产品取舍与最终确认；Codex 负责解释、实现、验证和如实汇报。
- Codex 不擅自扩大范围、引入当前小功能不需要的依赖，或自动进入下一小步。
- 只有当两个部分分别形成独立业务闭环时，才讨论拆分 Stage；调整前必须先取得用户确认。
- 开始每个 Stage 前，先阅读项目根目录中与 V1 相关的需求、功能模块、技术框架、技术验证和 Stage 划分文档，并检查当前 Git 状态；`AGENTS.md` 负责协作规则，项目文档负责业务和技术事实，不得仅凭当前 Chat 推断需求。
- 不得为了“工程化”“以后可能需要”或简历技术栈提前引入技术；只有当前 Stage 的真实业务需求已经出现时才能引入。

## 2. Stage 边界

| Stage | 业务闭环 | 本 Stage 边界 |
|---|---|---|
| 0 | 后端工程骨架 + 数据持久化地基 | 建立 FastAPI、PostgreSQL、SQLAlchemy、Alembic、Pydantic、pytest 和 Trip 最小 API；不接前端、高德、LLM 或生产部署。 |
| 1 | 高德地图事实层工程化 | 封装地点、路线、Polyline 和补能 POI 等真实地图事实；不做多日规划、Agent、补能决策或正式地图前端。 |
| 2 | 多轮需求理解 + Trip State Agent | 把自然语言持续合并为可持久化的 Trip State，并补问必要缺失信息；不做多日分组、完整路线或目的地推荐。 |
| 3 | 门到门公共交通完整闭环 | 在用户已给定每日地点的前提下，生成真实、完整的门到门公共交通路线；不决定地点分天，不做自驾补能或动态重规划。 |
| 4 | 多日地点规划引擎 | 根据酒店、天数、地点和硬约束完成分天、日内排序及真实路线生成；不做内容推荐或无必要的复杂求解器。 |
| 5 | 自驾 + 油车/电车补能规划 | 根据车辆与续航约束选择真实、可达的补能点并插入完整 Trip；不自建导航、专业能耗模型或价格优化。 |
| 6 | 用户修改 + 动态重规划 | 识别状态变化及影响范围，只重算受影响部分；不做多人协作、实时位置感知或外部事件自动触发。 |
| 7 | 完整产品交互 + 前后端联调 | 完成正式前端、地图展示、对话、规划进度及修改重规划闭环；不做社交分享、复杂导出或内容社区。 |
| 8 | 系统级质量验证 + 生产加固 | 全量回归、真实场景、异常、Agent Eval、日志及调用成本验证；不在此阶段新增大业务功能，也不把此前测试推迟到此处。 |
| 9 | Railway 真实生产上线 | 完成生产配置、迁移、Secret、日志、备份恢复、重新部署和上线 Smoke Test；不自建服务器、Kubernetes 或高可用集群。 |

## 3. 每个 Stage 的小步推进规则

- Stage 内一次只实现一个可验证的小功能。
- 实现前，Codex 必须先输出以下内容并等待用户明确确认：
  1. 本次小功能及实现目的。
  2. 为什么现在做。
  3. 增加、删除、修改的文件或模块范围。
  4. 首次出现的新名词及其通俗解释；没有则写“无”。
  5. 新增依赖及用途；没有则写“无”。
  6. 自动验证方式、可能需要的手动验证和本次边界。
- 用户确认后才开始实现；若实施中发现范围明显变化，停止并重新说明、确认。
- 实现完成后，Codex 自动运行能够可靠执行的 `pytest` 及与改动直接相关的检查，并汇报：执行命令、测试数量、通过/失败结果，以及本次重点覆盖的函数、接口和场景；不得只汇报“全部通过”。
- 如果必须由用户手动测试，Codex 必须写明前置条件、操作步骤、预期结果和注意事项；无需手测时明确写“无需人工验证”。
- 完成汇报后停止，不自动开始下一小功能。

## 4. Stage 交接信息格式

每个 Stage 通过 Stage 级验收后，在 Chat 中按以下格式交接：

```text
Stage：Stage N — 名称
完成目标：<本 Stage 已形成的可验收业务闭环>
主要增删改：<关键文件、模块、接口或数据结构>
自动测试：<执行的命令、测试数量与结果>
需要人工验证：<无需人工验证；或前置条件、步骤、预期结果、注意事项>
已知问题/遗留项：<无；或未解决事项及影响>
关键决策与约束：<后续必须继续遵守的设计决定和边界>
下一 Stage 目标：<下一 Stage 要形成的业务闭环>
下一 Stage 明确不做：<下一 Stage 的主要排除项>
```

交接信息必须以真实运行结果为依据；最后一个小功能完成不等于 Stage 完成，仍需先完成 Stage 级验收。
